import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import { z } from "zod";
//#region src/routes/auth.tsx
var $$splitComponentImporter = () => import("./auth-Dah5GtdQ.js");
var AuthSearch = z.object({ next: z.string().optional() });
var Route = createFileRoute("/auth")({
	validateSearch: AuthSearch,
	head: () => ({ meta: [{ title: "Sign in — AI Interview Simulator" }, {
		name: "description",
		content: "Sign in or create your AI Interview Simulator account to unlock your AI career workflow."
	}] }),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
